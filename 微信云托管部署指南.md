# 微信云托管部署指南

## 🌟 微信云托管优势

### 完美解决域名问题
- ✅ **自动配置合法域名**：无需手动添加到白名单
- ✅ **官方服务**：微信自家云服务，100%兼容
- ✅ **HTTPS自动配置**：无需购买SSL证书
- ✅ **无需域名校验**：天然支持小程序调用

### 技术优势
- 🚀 **1分钟部署**：比其他平台更快
- 💰 **按量付费**：有免费额度
- 🔧 **任意语言**：支持Node.js、Python、Java等
- 📊 **实时监控**：完整的日志和监控

## 🚀 部署步骤

### 第1步：访问微信云托管
1. 访问：https://cloud.weixin.qq.com/
2. 用微信扫码登录
3. 点击 **"免费试用"**

### 第2步：创建环境
1. 选择 **"创建环境"**
2. 环境名称：`word-memo-env`
3. 地域：选择离你最近的（如华南-广州）
4. 点击 **"创建"**

### 第3步：创建服务
1. 点击 **"创建服务"**
2. 服务名称：`word-memo-server`
3. 选择 **"代码包部署"**

### 第4步：准备代码包
需要创建一个包含Dockerfile的代码包：

#### 创建Dockerfile
```dockerfile
FROM node:18-alpine

WORKDIR /app

COPY package*.json ./
RUN npm install

COPY . .

EXPOSE 80

CMD ["npm", "start"]
```

#### 修改package.json
```json
{
  "scripts": {
    "start": "node app.js"
  }
}
```

#### 修改app.js监听端口
```javascript
const PORT = process.env.PORT || 80; // 微信云托管使用80端口
```

### 第5步：打包上传
1. 将server目录打包为zip文件
2. 在微信云托管控制台上传zip包
3. 等待构建完成（约2-3分钟）

### 第6步：配置版本
1. 设置 **CPU**: 0.25核
2. 设置 **内存**: 0.5GB
3. 设置 **实例数**: 1个
4. 点击 **"部署"**

### 第7步：获取访问地址
部署成功后，你会获得类似这样的域名：
```
https://word-memo-server-xxx.service.tcloudbase.com
```

## 🔧 配置环境变量

在微信云托管控制台：
1. 进入服务详情
2. 点击 **"环境变量"**
3. 添加变量：
```
NODE_ENV=production
JWT_SECRET=word_memo_jwt_secret_key_2024_very_secure_random_string_123456789
```

## 📱 更新小程序配置

```javascript
// utils/api.js
const API_CONFIGS = {
  local: 'http://localhost:3000/api',
  wechat: 'https://word-memo-server-xxx.service.tcloudbase.com/api', // 微信云托管域名
  render: 'https://word-memo-server.onrender.com/api'
};

const API_BASE_URL = API_CONFIGS.wechat; // 使用微信云托管
```

## 💰 费用说明

### 免费额度（每月）
- **CPU时间**: 1000 GHz·秒
- **内存时间**: 2000 GB·秒  
- **流量**: 1GB
- **构建时长**: 100分钟

### 超出费用
- **CPU**: 约0.00011元/GHz·秒
- **内存**: 约0.00003元/GB·秒
- 个人使用基本在免费额度内

## 🎯 部署优势

### vs Render
- ✅ **无域名配置**：自动支持小程序
- ✅ **更快速度**：国内服务器
- ✅ **官方支持**：微信自家服务

### vs 自建服务器
- ✅ **无需运维**：自动扩缩容
- ✅ **高可用**：99.9%可用性
- ✅ **安全性**：企业级安全

## 🔍 监控和调试

### 查看日志
1. 进入服务详情
2. 点击 **"日志"** 选项卡
3. 实时查看应用日志

### 性能监控
1. 查看CPU、内存使用情况
2. 监控请求量和响应时间
3. 设置告警规则

## 🚨 注意事项

### 端口配置
- 微信云托管使用80端口
- 需要修改app.js中的端口配置

### 文件结构
确保上传的zip包包含：
- package.json
- app.js
- Dockerfile
- 所有依赖文件

### 构建时间
- 首次部署约需要3-5分钟
- 后续更新约需要1-2分钟

## 🎉 完成检查

部署成功后确认：
- [ ] 服务状态为"运行中"
- [ ] 可以访问健康检查接口
- [ ] 小程序能正常调用API
- [ ] 无域名校验错误