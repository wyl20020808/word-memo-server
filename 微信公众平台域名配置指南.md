# 微信公众平台域名配置指南

## 🎯 配置合法域名步骤

### 第1步：登录微信公众平台
1. 访问 https://mp.weixin.qq.com/
2. 用你的微信扫码登录
3. 进入小程序管理后台

### 第2步：进入开发设置
1. 点击左侧菜单 **"开发"**
2. 选择 **"开发管理"**
3. 点击 **"开发设置"**

### 第3步：配置服务器域名
在 **"服务器域名"** 部分：

1. **request合法域名**：
   ```
   https://word-memo-server.onrender.com
   ```

2. **socket合法域名**（如果需要WebSocket）：
   ```
   wss://word-memo-server.onrender.com
   ```

3. **uploadFile合法域名**（如果需要上传文件）：
   ```
   https://word-memo-server.onrender.com
   ```

4. **downloadFile合法域名**（如果需要下载文件）：
   ```
   https://word-memo-server.onrender.com
   ```

### 第4步：保存配置
1. 点击 **"保存并提交"**
2. 等待审核（通常几分钟到几小时）

## ⚠️ 重要注意事项

### 域名要求
- ✅ 必须是HTTPS协议
- ✅ 必须是有效的域名
- ✅ 必须能正常访问
- ❌ 不能是IP地址
- ❌ 不能是localhost

### 配置限制
- 每个类型最多配置20个域名
- 域名不支持通配符
- 配置后立即生效（开发版）
- 正式版需要重新发布

## 🔍 验证配置

### 检查域名是否生效
1. 在小程序中发起网络请求
2. 查看控制台是否还有域名错误
3. 测试API调用是否成功

### 如果配置失败
可能的原因：
- 域名格式不正确
- 服务器无法访问
- HTTPS证书问题

## 📱 测试步骤

配置完成后：
1. 重新编译小程序
2. 测试网络请求
3. 检查功能是否正常

## 🚨 常见问题

### Q: 配置后还是无法访问？
A: 等待几分钟，配置可能需要时间生效

### Q: 提示域名无法访问？
A: 检查域名是否正确，服务器是否正常运行

### Q: 可以配置多个域名吗？
A: 可以，每行一个域名，最多20个