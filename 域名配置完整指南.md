# 域名配置完整指南

## 方案1：购买域名 + Cloudflare（推荐，约50元/年）

### 1. 购买域名
**推荐域名注册商**：
- 阿里云：https://wanwang.aliyun.com/ （约50-80元/年）
- 腾讯云：https://dnspod.cloud.tencent.com/ （约45-70元/年）
- Namecheap：https://www.namecheap.com/ （约60-100元/年）

**域名选择建议**：
- `.com` 域名：约70元/年
- `.cn` 域名：约50元/年  
- `.top` 域名：约20元/年（便宜但可能被限制）

### 2. 使用Cloudflare免费SSL和CDN
1. 注册 https://cloudflare.com/
2. 添加你的域名
3. 修改域名的DNS服务器为Cloudflare提供的
4. 在Cloudflare中添加A记录指向你的公网IP：183.220.117.142
5. 开启SSL/TLS -> Full模式
6. 这样你就有了 https://yourdomain.com

### 3. 配置步骤详解

#### 步骤1：购买域名
假设你购买了域名：`wordmemo.top`

#### 步骤2：Cloudflare配置
```
DNS记录配置：
类型: A
名称: @
内容: 183.220.117.142
代理状态: 已代理（橙色云朵）

类型: A  
名称: api
内容: 183.220.117.142
代理状态: 已代理（橙色云朵）
```

#### 步骤3：SSL配置
- SSL/TLS -> 概述 -> 选择"完全"模式
- SSL/TLS -> 边缘证书 -> 开启"始终使用HTTPS"

#### 步骤4：修改小程序API地址
```javascript
const API_BASE_URL = 'https://api.wordmemo.top/api';
// 或者
const API_BASE_URL = 'https://wordmemo.top/api';
```

## 方案2：免费内网穿透（临时方案）

### 使用ngrok（免费版有限制）
```bash
# 1. 下载ngrok: https://ngrok.com/
# 2. 注册账号获取token
# 3. 启动穿透
ngrok http 3000

# 会得到类似这样的地址：
# https://abc123.ngrok.io
```

**限制**：
- 每次重启URL会变化
- 免费版有连接数限制
- 速度可能较慢

### 使用花生壳（国内服务）
1. 注册 https://hsk.oray.com/
2. 下载客户端
3. 配置内网穿透
4. 获得固定域名（免费版有限制）

## 方案3：使用免费云服务（推荐新手）

### Railway（免费500小时/月）
1. 注册 https://railway.app/
2. 连接GitHub仓库
3. 自动部署获得 https://xxx.railway.app 域名
4. 直接可用，无需额外配置

### Render（免费版）
1. 注册 https://render.com/
2. 连接GitHub仓库  
3. 获得 https://xxx.onrender.com 域名

## 当前最佳方案推荐

### 立即可用方案：Railway
1. 把你的server代码上传到GitHub
2. 在Railway中连接仓库
3. 自动部署后获得HTTPS域名
4. 修改小程序API地址

### 长期方案：购买域名
1. 购买便宜域名（约50元/年）
2. 使用Cloudflare免费SSL
3. 配置DNS解析
4. 获得专属域名

## 快速实施步骤

### 选择Railway方案（最快）
```bash
# 1. 创建GitHub仓库
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/yourusername/word-memo-server.git
git push -u origin main

# 2. 在Railway中部署
# - 访问 railway.app
# - 连接GitHub
# - 选择仓库
# - 自动部署
```

### 选择域名方案（推荐）
1. **购买域名**：在阿里云购买 `.top` 域名（约20元/年）
2. **配置Cloudflare**：免费SSL + CDN
3. **DNS解析**：指向你的公网IP
4. **修改API地址**：使用HTTPS域名

## 配置文件模板

### 环境变量配置
```env
# .env
NODE_ENV=production
PORT=3000
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=word_memo
JWT_SECRET=your_jwt_secret
DOMAIN=https://yourdomain.com
```

### Nginx配置（如果使用云服务器）
```nginx
server {
    listen 80;
    server_name yourdomain.com;
    return 301 https://$server_name$request_uri;
}

server {
    listen 443 ssl;
    server_name yourdomain.com;
    
    ssl_certificate /path/to/cert.pem;
    ssl_certificate_key /path/to/key.pem;
    
    location /api/ {
        proxy_pass http://localhost:3000/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

## 成本对比

| 方案 | 年费用 | 优点 | 缺点 |
|------|--------|------|------|
| Railway免费版 | 0元 | 立即可用，自动HTTPS | 500小时/月限制 |
| 域名+Cloudflare | 50元 | 完全控制，无限制 | 需要配置 |
| 云服务器 | 300元 | 最稳定 | 成本较高 |
| ngrok免费版 | 0元 | 快速测试 | URL会变化 |

## 推荐实施顺序

1. **立即测试**：使用Railway部署，获得临时域名
2. **长期使用**：购买便宜域名，配置Cloudflare
3. **生产环境**：考虑云服务器方案

选择哪个方案？我建议先用Railway快速测试，然后购买域名做长期方案。