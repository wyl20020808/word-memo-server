# 语音识别故障排查指南

## 🔍 问题诊断流程

```
问题出现
    ↓
第1步：检查部署状态
    ↓
第2步：检查环境变量
    ↓
第3步：查看云函数日志
    ↓
第4步：检查百度配额
    ↓
问题解决
```

---

## 🚨 常见问题及解决方案

### 问题1：部署失败 - "依赖安装失败"

**症状**：
- 点击"上传并部署"后显示失败
- 错误信息：依赖安装失败

**原因**：
- 网络连接不稳定
- 代码有语法错误
- 微信开发者工具版本过旧

**解决方案**：

1. **检查网络连接**
   ```
   确保网络正常，尝试访问其他网站
   ```

2. **检查代码语法**
   ```
   在编辑器中查看是否有红色错误提示
   如果有，检查代码是否完整复制
   ```

3. **删除并重新创建**
   ```
   1. 右键voiceToText函数 → 删除
   2. 等待删除完成
   3. 重新创建新的Node.js云函数
   4. 重新复制代码
   5. 重新部署
   ```

4. **更新微信开发者工具**
   ```
   检查是否有新版本可用
   更新到最新版本后重试
   ```

---

### 问题2：识别不工作 - "语音识别暂不可用"

**症状**：
- 长按🎤能录音
- 松开后显示"语音识别暂不可用"
- 输入框没有识别结果

**原因**：
- 环境变量配置错误
- API Key或Secret Key不正确
- 百度账户配额用尽
- 云函数执行失败

**解决方案**：

1. **检查环境变量**
   ```
   1. 微信开发者工具 → 云开发 → 云函数
   2. 右键voiceToText → 函数配置
   3. 检查环境变量：
      - BAIDU_API_KEY 是否存在
      - BAIDU_SECRET_KEY 是否存在
      - 值是否正确（没有多余空格）
   ```

2. **验证百度凭证**
   ```
   1. 访问 https://ai.baidu.com/
   2. 登录 → 控制台 → 应用列表
   3. 找到你的应用
   4. 复制 API Key 和 Secret Key
   5. 与环境变量中的值对比
   6. 如果不同，更新环境变量
   7. 重新部署
   ```

3. **查看云函数日志**
   ```
   1. 微信开发者工具 → 云开发 → 云函数
   2. 右键voiceToText → 查看日志
   3. 查看最近的错误信息
   4. 根据错误信息判断问题
   ```

4. **检查百度配额**
   ```
   1. 访问 https://ai.baidu.com/
   2. 登录 → 控制台 → 应用列表
   3. 点击你的应用
   4. 查看"配额"或"使用情况"
   5. 如果配额用尽，升级为付费版本
   ```

---

### 问题3：识别结果为空

**症状**：
- 录音成功
- 云函数执行成功
- 但识别结果为空
- 输入框没有文字

**原因**：
- 录音时间太短
- 音频质量太差
- 百度API无法识别
- 网络连接不稳定

**解决方案**：

1. **确保录音时间足够**
   ```
   - 最少录音1秒
   - 最好录音3-5秒
   - 说话要清晰
   ```

2. **改善音频质量**
   ```
   - 在安静的环境中录音
   - 避免背景噪音
   - 靠近麦克风说话
   - 说话速度不要太快
   ```

3. **尝试重新录音**
   ```
   - 松开按钮后等待2-3秒
   - 再次长按🎤录音
   - 说不同的内容
   - 观察是否能识别
   ```

4. **检查网络连接**
   ```
   - 确保网络正常
   - 尝试切换WiFi或移动网络
   - 重新测试
   ```

---

### 问题4：显示"缺少百度语音凭证"

**症状**：
- 云函数日志显示"缺少百度语音凭证"
- 识别失败

**原因**：
- 环境变量未配置
- 环境变量名称错误
- 环境变量值为空

**解决方案**：

1. **检查环境变量是否存在**
   ```
   1. 打开函数配置
   2. 查看环境变量列表
   3. 确保有以下两个变量：
      - BAIDU_API_KEY
      - BAIDU_SECRET_KEY
   ```

2. **检查环境变量名称**
   ```
   确保变量名称完全正确：
   - 不能是 baidu_api_key（小写）
   - 不能是 BAIDU_API_KEY_（多余下划线）
   - 必须是 BAIDU_API_KEY（大写）
   ```

3. **检查环境变量值**
   ```
   1. 打开函数配置
   2. 点击编辑环境变量
   3. 确保值不为空
   4. 确保没有多余的空格
   5. 确保值完全正确
   ```

4. **重新配置并部署**
   ```
   1. 删除所有环境变量
   2. 重新添加环境变量
   3. 确保值正确
   4. 点击"上传并部署"
   ```

---

### 问题5：显示"获取百度token失败"

**症状**：
- 云函数日志显示"获取百度token失败"
- 识别失败

**原因**：
- API Key或Secret Key不正确
- 百度API服务不可用
- 网络连接问题

**解决方案**：

1. **验证百度凭证**
   ```
   1. 访问 https://ai.baidu.com/
   2. 登录 → 控制台 → 应用列表
   3. 找到你的应用
   4. 复制 API Key 和 Secret Key
   5. 与环境变量中的值对比
   6. 如果不同，更新环境变量
   ```

2. **检查百度API状态**
   ```
   1. 访问 https://ai.baidu.com/
   2. 查看是否有服务公告
   3. 检查API是否正常运行
   ```

3. **重新部署**
   ```
   1. 更新环境变量
   2. 点击"上传并部署"
   3. 等待部署完成
   4. 重新测试
   ```

---

### 问题6：显示"下载文件失败"

**症状**：
- 云函数日志显示"下载文件失败"
- 识别失败

**原因**：
- 音频文件路径错误
- 云存储权限问题
- 文件不存在

**解决方案**：

1. **检查前端代码**
   ```
   确保前端正确上传文件到云存储
   检查 components/ai-assistant/ai-assistant.js
   ```

2. **检查云存储权限**
   ```
   1. 微信开发者工具 → 云开发 → 存储
   2. 检查权限设置
   3. 确保云函数有读取权限
   ```

3. **查看详细日志**
   ```
   1. 查看云函数日志
   2. 查看具体的错误信息
   3. 根据错误信息判断问题
   ```

---

### 问题7：识别结果不准确

**症状**：
- 识别成功
- 但识别结果不是说的内容
- 识别结果有错别字

**原因**：
- 音频质量差
- 口音问题
- 背景噪音
- 说话速度太快

**解决方案**：

1. **改善录音环境**
   ```
   - 在安静的地方录音
   - 避免背景音乐或噪音
   - 靠近麦克风
   ```

2. **改善说话方式**
   ```
   - 说话清晰
   - 说话速度适中
   - 避免口音过重
   - 避免方言
   ```

3. **尝试不同的内容**
   ```
   - 测试不同的单词
   - 测试不同的句子
   - 观察识别准确率
   ```

4. **检查百度API设置**
   ```
   - 访问百度AI平台
   - 查看语音识别的参数设置
   - 调整识别参数
   ```

---

## 🛠️ 调试技巧

### 1. 查看云函数日志

```
微信开发者工具 → 云开发 → 云函数
→ 右键voiceToText → 查看日志
```

日志会显示：
- 函数执行时间
- 输入参数
- 输出结果
- 错误信息

### 2. 测试云函数

```
微信开发者工具 → 云开发 → 云函数
→ 右键voiceToText → 测试
```

输入测试参数：
```json
{
  "filePath": "cloud://your-env-id/test-audio.wav"
}
```

### 3. 检查环境变量

```
微信开发者工具 → 云开发 → 云函数
→ 右键voiceToText → 函数配置
→ 查看环境变量
```

### 4. 查看云存储

```
微信开发者工具 → 云开发 → 存储
→ 查看上传的文件
```

---

## 📊 问题诊断表

| 症状 | 可能原因 | 解决方案 |
|------|--------|--------|
| 部署失败 | 网络问题或代码错误 | 检查网络，删除重建 |
| 识别不工作 | 环境变量错误 | 检查API Key和Secret Key |
| 识别为空 | 录音太短或百度配额用尽 | 重新录音或升级配额 |
| 缺少凭证 | 环境变量未配置 | 重新配置并部署 |
| Token失败 | 凭证不正确 | 验证百度凭证 |
| 下载失败 | 文件路径错误 | 检查前端代码 |
| 结果不准 | 音频质量差 | 改善录音环境 |

---

## 📞 获取帮助

1. **查看云函数日志**
   - 最直接的问题诊断方法
   - 通常能看到具体的错误信息

2. **查看相关文档**
   - `VOICE_RECOGNITION_DEPLOYMENT_STEPS.md` - 部署指南
   - `VOICE_RECOGNITION_VISUAL_GUIDE.md` - 可视化指南

3. **检查百度API**
   - 访问 https://ai.baidu.com/
   - 查看应用状态和配额

4. **检查微信云开发**
   - 查看云函数是否正常运行
   - 查看云存储权限设置

---

## ✅ 验证清单

如果遇到问题，按以下顺序检查：

- [ ] 云函数是否部署成功
- [ ] 环境变量是否正确配置
- [ ] API Key和Secret Key是否正确
- [ ] 百度账户是否有足够配额
- [ ] 网络连接是否正常
- [ ] 录音时间是否足够长
- [ ] 音频质量是否良好
- [ ] 云函数日志是否有错误信息

---

**如果问题仍未解决，请查看云函数日志获取更多信息。** 🔍

