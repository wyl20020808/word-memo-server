# 语音识别部署可视化指南

## 📸 第1步：获取百度API凭证

### 步骤1.1：访问百度AI平台
```
打开浏览器 → 输入 https://ai.baidu.com/ → 回车
```

### 步骤1.2：登录或注册
```
点击右上角"登录" → 输入百度账号密码 → 登录
（如果没有账号，点击"注册"创建新账号）
```

### 步骤1.3：进入控制台
```
登录后 → 点击右上角"控制台" → 进入控制台页面
```

### 步骤1.4：创建应用
```
左侧菜单 → 找到"应用列表" → 点击进入
→ 点击蓝色"创建应用"按钮
```

### 步骤1.5：填写应用信息
```
应用名称：微信小程序语音识别（或任意名称）
应用类型：选择"服务端"
应用描述：可选，随意填写
→ 点击"立即创建"
```

### 步骤1.6：获取凭证
```
应用创建成功后，页面显示：
- API Key: abc123def456...（复制这个）
- Secret Key: xyz789uvw012...（复制这个）

用记事本保存这两个值
```

**✅ 第1步完成！**

---

## ☁️ 第2步：在微信开发者工具中部署

### 步骤2.1：打开微信开发者工具
```
启动微信开发者工具 → 打开你的小程序项目
```

### 步骤2.2：进入云开发
```
顶部菜单栏 → 找到"云开发"选项卡 → 点击进入
```

### 步骤2.3：进入云函数管理
```
左侧菜单 → 找到"云函数" → 点击展开
```

### 步骤2.4：创建新云函数
```
在"云函数"上右键点击
→ 选择"新建 Node.js 云函数"
→ 输入函数名称：voiceToText
→ 点击"确定"
```

**等待几秒钟，新的云函数会自动打开编辑器**

### 步骤2.5：复制代码
```
1. 打开文件：server/cloud-functions/voiceToText/index.js
2. 全选所有代码（Ctrl+A）
3. 复制代码（Ctrl+C）
4. 回到微信开发者工具的编辑器
5. 删除所有默认代码
6. 粘贴新代码（Ctrl+V）
7. 保存（Ctrl+S）
```

### 步骤2.6：配置环境变量
```
1. 在编辑器上方找到"函数配置"按钮
2. 点击打开配置面板
3. 找到"环境变量"部分
4. 点击"添加"按钮

添加第一个环境变量：
- 变量名：BAIDU_API_KEY
- 变量值：粘贴你的 API Key
- 点击"确定"

添加第二个环境变量：
- 变量名：BAIDU_SECRET_KEY
- 变量值：粘贴你的 Secret Key
- 点击"确定"

5. 关闭配置面板
```

### 步骤2.7：上传并部署
```
1. 在编辑器上方找到"上传并部署"按钮
2. 点击"上传并部署：云端安装依赖"
3. 等待部署完成（30-60秒）
4. 看到成功提示即可
```

**✅ 第2步完成！**

---

## 🧪 第3步：测试功能

### 步骤3.1：打开小程序
```
在微信开发者工具中点击"预览"或"真机调试"
```

### 步骤3.2：进入背单词页面
```
小程序首页 → 点击"背单词"进入
```

### 步骤3.3：打开AI助手
```
页面右下角 → 点击🤖 AI助手按钮
```

### 步骤3.4：测试语音输入
```
1. 在AI助手窗口中找到输入框
2. 输入框右边有一个🎤按钮
3. 长按🎤按钮开始录音
4. 说一些话（例如"你好"或"apple"）
5. 松开按钮停止录音
6. 等待识别结果
```

### 步骤3.5：查看结果
```
如果成功：
- 识别出的文字会出现在输入框中
- 可以继续编辑或直接发送

如果失败：
- 会显示"识别失败，请重试"
- 检查网络连接
- 检查环境变量配置
```

**✅ 第3步完成！**

---

## 🔍 调试技巧

### 查看云函数日志
```
1. 微信开发者工具 → 云开发 → 云函数
2. 右键点击"voiceToText"函数
3. 选择"查看日志"
4. 查看错误信息
```

### 常见日志错误

**错误1：缺少百度语音凭证**
```
原因：环境变量未配置或配置错误
解决：重新检查环境变量，确保没有空格
```

**错误2：获取百度token失败**
```
原因：API Key或Secret Key错误
解决：重新复制百度凭证，确保完全正确
```

**错误3：下载文件失败**
```
原因：音频文件路径错误
解决：检查前端是否正确上传文件
```

---

## 📋 检查清单

部署前检查：
- [ ] 已获取百度 API Key
- [ ] 已获取百度 Secret Key
- [ ] 已打开微信开发者工具
- [ ] 小程序已开通云开发

部署中检查：
- [ ] 已创建 voiceToText 云函数
- [ ] 已复制云函数代码
- [ ] 已配置 BAIDU_API_KEY 环境变量
- [ ] 已配置 BAIDU_SECRET_KEY 环境变量
- [ ] 已点击"上传并部署"

部署后检查：
- [ ] 部署显示成功
- [ ] 云函数列表中能看到 voiceToText
- [ ] 在小程序中能打开AI助手
- [ ] 能看到🎤语音输入按钮
- [ ] 语音识别功能正常工作

---

## 💬 获取帮助

如果遇到问题：

1. **查看云函数日志**
   - 右键云函数 → 查看日志 → 查看错误信息

2. **检查环境变量**
   - 打开函数配置 → 验证 API Key 和 Secret Key
   - 确保没有多余空格

3. **检查百度配额**
   - 访问 https://ai.baidu.com/
   - 进入控制台 → 查看应用配额
   - 免费版每天500次调用

4. **重新部署**
   - 如果修改了代码或环境变量
   - 需要重新点击"上传并部署"

---

## 🎉 成功标志

当你看到以下现象时，说明部署成功：

✅ 云函数部署显示成功
✅ 小程序中能看到🎤语音输入按钮
✅ 长按🎤能开始录音
✅ 录音完成后能识别出文字
✅ 识别结果出现在输入框中

祝你部署顺利！

